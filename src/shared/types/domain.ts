export type CompetitionSummary = {
  id: string;
  name: string;
  date: string;
  location: string;
};

export type PostSummary = {
  id: string;
  title: string;
  author: string;
  createdAt: string;
};
